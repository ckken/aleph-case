# Deno Aleph Demo

## Install
+ `deno install -A -f -n aleph https://deno.land/x/aleph@v0.2.27/cli.ts`
+ `export PATH="/Users/ken/.deno/bin:$PATH"`

## Command
```cmd
# SSR dev
aleph dev
# SSR prod
aleph dev
# SSG
aleph dev
```